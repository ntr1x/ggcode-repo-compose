print(debug.info(1, "Hello"))

print(yaml:stringify({
  bundle = bundle,
  clients = clients,
  groups = groups,
  realm = realm,
  roles = roles,
  service = service,
  users = users,
  members = members,
  grants = grants
}))
