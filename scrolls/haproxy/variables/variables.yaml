compose:
  profiles:
    - default
  networks:
    default:
haproxy:
  frontend:
    any:
      mode: http
      bind: ":1-21,:23-8403,:8405-10000"
      default_backend: gw
  backend:
    gw:
      mode: http
      server: s1 host.docker.internal
